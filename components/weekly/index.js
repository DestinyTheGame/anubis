import React, { Component } from 'react';

/**
 * Render weekly stats.
 *
 * @param {Object} props Properties.
 * @private
 */
export default function weekly(props) {
  const { currentMap, currentWeek } = props;

  return (
    <div className='report'>
    </div>
  );
}
